export enum ContentPrefix {
    ASSET = 'asset',
    BACKUP = 'backup'
}

export enum ContentType {
    TEXT = 'text/plain',
    JSON = 'application/json',
    JPEG = 'image/jpeg',
    PNG = 'image/png',
    GIF = 'image/gif',
    BMP = 'image/bmp',
    WEBP = 'image/webp',
    TIFF = 'image/tiff',
    SVG = 'image/svg+xml'
}